import * as React from 'react';
import { Box } from '@mui/material';

export default function BlankPageWithLayout() {
  return (
    <Box
      sx={{
        width: '100%',
        height: '100vh',
        backgroundColor: 'white', // Set the background to white or any other color you prefer
      }}
    />
  );
}
